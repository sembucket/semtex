#
# Make flags for CRAY
#
# $Id$

set fc     = "cf77"
set optfc  = "-dp"
set system = ("-lf -lcps" $system)

if ($debug) then
    set opt = "-g"
endif

if ($parallel) then
	set cc = "mpicc"
	set fc = "mpif77"
endif

