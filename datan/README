                                ACOR OPTIONS

    usage: acor [options] [input]
    [options]:
    -d          ... create dimensionless autocorrelation
    -h          ... display this message
    -m          ... remove mean value before computation
    -n <#lags>  ... compute this many lags               [Default: 32]
    -o <output> ... write output to named file           [Default: stdout]
    -r <rate>   ... Sampling rate [Hz]                   [Default: 1 Hz]
    -v          ... set verbose (add header to output)


                               ASP OPTIONS

Usage: asp [options] [file]
  options are:
  -h             ... print this message
  -b blocksize   ... size of data segments         [D: 512]
  -m             ... switch off mean removal
  -o             ... switch off overlap averaging
  -r rate        ... sampling rate                 [D: 1Hz]
  -v             ... verbose output
  -w window      ... one of "none", "hann", "gauss", "cost"

                              CHOP OPTIONS

    usage: chop [options] [input]
    [options]:
    -h                 ... print this message
    -s <start line>    ... start at this line of input  [Default: 1]
    -n <#lines>        ... reproduce this many lines    [Default: to EOF]

   

                              SLIT OPTIONS

    usage: slit [options] [input]
    [options]:
    -h                  ... print this message
    -c col1[,col2,...]  ... reproduce these columns of file [Default: 1]


                               PDF OPTIONS

    usage: pdf [options] [input]
    [options]:
    -h          ... display this message      
    -n <#bins>  ... create PDF with #bins bins          [Default: 21]
    -o <output> ... write output to named file          [Default: stdout]
    -v          ... set verbose (add header to output)

