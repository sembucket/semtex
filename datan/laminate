##############################################################################
#  laminate: print files named on comand line in columns.
#
#  Usage : laminate file1 file2 file3 ... fileN
#
#  Bugs:  Prints trailing blank lines if there is only one argument.
#         Fails if there are no arguments.
#         Fails if one of args is not a valid file name.
#
# $Id$
#############################################################################

pr -m -t -s" " $* | sed '/^[ 	][ 	]*$/d'
